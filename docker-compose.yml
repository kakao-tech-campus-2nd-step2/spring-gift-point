version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - redis
    environment:
      - SPRING_REDIS_HOST=redis
      -
    ports:
      - "8080:8080"

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"
